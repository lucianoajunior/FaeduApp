correctDiagram = ( exercise, submission ) => {
	
	const listVerbs = ['Manter', 'Gerenciar', 'Manutenir'];
	const lastLetter = () => {
		return str.charAt( str.length - 1 );
	};

	const exerciseJSON = exercise.json;
	const submissionJSON = submission.json;


	console.log( exercise.json )

	console.log( submission.json )

	return [];
}

module.exports = correctDiagram;